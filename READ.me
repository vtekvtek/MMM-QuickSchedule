# MMM-QuickSchedule

A MagicMirror module that logs into QuickService Schedule, scrapes the SingleTechScheduleList page for a named tech, writes an all-day ICS for the current week (Mon to Sun), and displays the week as a bottom bar strip.

## Setup

### 1) Install
Copy this folder into:

`MagicMirror/modules/MMM-QuickSchedule`

Then run:

```bash
cd MagicMirror/modules/MMM-QuickSchedule
npm install